// Lobby.cpp
// Created by Robin Rowe 2021-08-15
// MIT Open Source

#include "Lobby.h"
using namespace std;

ostream& Lobby::Print(ostream& os) const
{	// to-do
	return os << "Lobby";
} 

istream& Lobby::Input(std::istream& is) 
{	// to-do
	return is;
}
